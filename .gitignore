# Python bytecode caches generated by importing the repo-root shared modules
# (frac_metrics.py, case_registry.py) from tests / smoke checks.
__pycache__/
*.pyc
.pytest_cache/

# uv-managed analysis virtualenv (PAPER-03 env); the pinned uv.lock is the
# committed reproducibility anchor, the materialized .venv is not.
.venv/
